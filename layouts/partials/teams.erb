<div class='four columns'>
<table class='schedule white'>
  <% @teams.each_with_index do |t,i| %>
    <tr>
      <th><%= i + 1 + @offset %>.</th>
      <td><%= t %></td>
    </tr>
  <% end %>
</table>
</div>
